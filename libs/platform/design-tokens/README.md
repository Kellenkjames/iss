# Design tokens

This package exposes the canonical ISS design primitives as CSS custom properties under the `--iss-` namespace.

Consumers can import the generated stylesheet from the package entry point or use the exported token contract from TypeScript.
